(()=>{const d=document.createElement('dialog');d.id='historyDialog';d.innerHTML=`<div class="history-panel"><div class="dialog-title"><div><small>最近 7 天按分钟聚合</small><h2 id="historyTitle">历史图表</h2></div><button class="ghost" onclick="historyDialog.close()">关闭</button></div><div class="history-tabs"><button data-hours="1">1 小时</button><button data-hours="24" class="active">24 小时</button><button data-hours="168">7 天</button></div><div class="chart-legend"><span class="cpu">CPU</span><span class="memory">内存</span><span class="disk">磁盘</span></div><canvas id="historyCanvas" width="1000" height="360"></canvas><p id="historyEmpty"></p></div>`;document.body.appendChild(d);let current='';window.openHistory=async id=>{current=id;$('historyTitle').textContent=(data.find(v=>v.id===id)?.name||id)+' · 历史图表';d.showModal();await load(24)};d.querySelectorAll('[data-hours]').forEach(b=>b.onclick=async()=>{d.querySelectorAll('[data-hours]').forEach(x=>x.classList.remove('active'));b.classList.add('active');await load(+b.dataset.hours)});async function load(hours){const points=await api(`/api/vps/${current}/history?hours=${hours}`);draw(points)}function draw(points){const canvas=$('historyCanvas'),ctx=canvas.getContext('2d'),w=canvas.width,h=canvas.height,pad=42;ctx.clearRect(0,0,w,h);ctx.strokeStyle='#263248';ctx.fillStyle='#8290a8';ctx.font='12px sans-serif';for(let i=0;i<=4;i++){const y=pad+(h-pad*2)*i/4;ctx.beginPath();ctx.moveTo(pad,y);ctx.lineTo(w-pad,y);ctx.stroke();ctx.fillText(`${100-i*25}%`,4,y+4)}if(!points.length){$('historyEmpty').textContent='暂无历史数据，Agent 上报一分钟后开始生成。';return}$('historyEmpty').textContent='';for(const[key,color]of [['cpu','#ef3270'],['memory','#50e3a4'],['disk','#6f8cff']]){ctx.strokeStyle=color;ctx.lineWidth=2;ctx.beginPath();points.forEach((p,i)=>{const x=pad+(w-pad*2)*(i/Math.max(points.length-1,1)),y=h-pad-(h-pad*2)*Math.min(Math.max(+p[key],0),100)/100;i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.stroke()}ctx.fillStyle='#8290a8';ctx.fillText(new Date(points[0].at).toLocaleString('zh-CN',{hour12:false}),pad,h-12);ctx.textAlign='right';ctx.fillText(new Date(points.at(-1).at).toLocaleString('zh-CN',{hour12:false}),w-pad,h-12);ctx.textAlign='left'}})();
