(()=>{const d=document.createElement('dialog');d.id='monitorDialog';d.innerHTML=`<form method="dialog" class="monitor-form"><div class="dialog-title"><div><small>TCP · HTTP · DNS · TLS</small><h2>服务监控</h2></div><button type="button" class="ghost" onclick="monitorDialog.close()">关闭</button></div><div id="monitorList" class="monitor-list"></div><hr><h3 id="monitorTitle">新增监控</h3><input id="monId" type="hidden"><div class="row"><label>名称<input id="monName" placeholder="网站 HTTPS"></label><label>关联 VPS<select id="monVps"><option value="">不关联</option></select></label></div><div class="row"><label>类型<select id="monType"><option>HTTP</option><option>TCP</option><option>DNS</option><option>TLS</option></select></label><label>目标<input id="monTarget" placeholder="https://example.com"></label></div><div class="row"><label>间隔（秒，最低 10）<input id="monInterval" type="number" min="10" value="60"></label><label>超时（秒）<input id="monTimeout" type="number" min="1" max="30" value="5"></label><label>连续失败几次告警<input id="monNotify" type="number" min="1" value="3"></label></div><div class="row"><label id="monExpectedLabel">预期 HTTP 状态码<input id="monExpected" type="number" value="200"></label><label id="monKeywordLabel">响应关键字（可选）<input id="monKeyword"></label></div><label class="switch-row"><span><b>启用监控</b><small>关闭后暂停检测和告警</small></span><input id="monEnabled" type="checkbox" checked><i></i></label><p id="monResult"></p><menu><button type="button" class="ghost" id="monReset">新增监控</button><button type="button" id="monSave">保存监控</button></menu></form>`;document.body.appendChild(d);let items=[];const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function name(id){return data.find(v=>v.id===id)?.name||id||'未关联'}function fill(){const options=data.map(v=>`<option value="${v.id}">${esc(v.name)}</option>`).join('');$('monVps').innerHTML='<option value="">不关联</option>'+options}function reset(){d.querySelector('form').reset();$('monId').value='';$('monInterval').value=60;$('monTimeout').value=5;$('monNotify').value=3;$('monExpected').value=200;$('monEnabled').checked=true;$('monitorTitle').textContent='新增监控';typeChanged();$('monResult').textContent=''}function typeChanged(){const t=$('monType').value;$('monExpectedLabel').hidden=t==='TCP'||t==='DNS';$('monKeywordLabel').hidden=t==='TCP'||t==='TLS';$('monExpectedLabel').firstChild.textContent=t==='TLS'?'证书到期告警天数':'预期 HTTP 状态码';$('monKeywordLabel').firstChild.textContent=t==='DNS'?'期望解析 IP（可选）':'响应关键字（可选）';if(t==='TLS'&&+$('monExpected').value===200)$('monExpected').value=14}function render(){$('monitorList').innerHTML=items.length?items.map(m=>`<div class="monitor-item"><span class="monitor-dot ${m.lastStatus}"></span><div><b>${esc(m.name)}</b><small>${m.type} · ${esc(m.target)} · ${esc(name(m.vpsId))}</small>${m.lastError?`<em>${esc(m.lastError)}</em>`:''}</div><div><strong>${m.lastStatus==='up'?'正常':m.lastStatus==='down'?'故障':'等待'}</strong><small>${m.lastLatencyMs||0}ms</small></div><div><button type="button" class="ghost" onclick="editMonitor(${m.id})">编辑</button> <button type="button" class="ghost danger" onclick="deleteMonitor(${m.id})">删除</button></div></div>`).join(''):'<div class="log-empty">尚未配置服务监控</div>'}async function load(){fill();items=await api('/api/vps/monitors');render()}window.editMonitor=id=>{const m=items.find(x=>x.id===id);if(!m)return;$('monId').value=m.id;$('monName').value=m.name;$('monVps').value=m.vpsId||'';$('monType').value=m.type;$('monTarget').value=m.target;$('monInterval').value=m.intervalSec;$('monTimeout').value=m.timeoutSec;$('monNotify').value=m.notifyAfter;$('monExpected').value=m.expectedStatus;$('monKeyword').value=m.keyword||'';$('monEnabled').checked=m.enabled;$('monitorTitle').textContent='编辑 '+m.name;typeChanged()};window.deleteMonitor=async id=>{if(!confirm('确定删除这个监控吗？'))return;await api('/api/vps/monitors/'+id,{method:'DELETE'});await load()};$('monType').onchange=typeChanged;$('monReset').onclick=reset;$('monSave').onclick=async()=>{const body={id:+$('monId').value||0,name:$('monName').value.trim(),vpsId:$('monVps').value,type:$('monType').value,target:$('monTarget').value.trim(),intervalSec:+$('monInterval').value,timeoutSec:+$('monTimeout').value,notifyAfter:+$('monNotify').value,expectedStatus:+$('monExpected').value,keyword:$('monKeyword').value.trim(),enabled:$('monEnabled').checked};try{await api('/api/vps/monitors',{method:'POST',body:JSON.stringify(body)});reset();await load();$('monResult').textContent='监控已保存'}catch(e){$('monResult').textContent=e.message}};const header=document.querySelector('header>div:last-child');const b=document.createElement('button');b.className='ghost';b.textContent='服务监控';b.onclick=async()=>{reset();d.showModal();try{await load()}catch(e){$('monResult').textContent=e.message}};header.appendChild(b)})();
